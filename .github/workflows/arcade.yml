name: Generate Pacman Animation

on:
workflow_dispatch:

permissions:
contents: write

jobs:
generate:
runs-on: ubuntu-latest

```
steps:
  - uses: actions/checkout@v4

  - name: Test
    run: echo "Workflow detected successfully"
```
